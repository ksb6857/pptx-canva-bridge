---
name: pptx-canva-bridge
description: 파워포인트로 만든 pptx 를 캔바에 올릴 때 글꼴 굵기가 한 단계 가늘어지거나 업로드 자체가 실패하는 문제를 잡는다. 녹화용 원본은 그대로 두고 캔바용 사본을 따로 만든다. 이름에 웨이트가 든 글꼴에 겹친 b 속성 제거, 임베드 글꼴·애니메이션 제거, zip CRC 손상 복구, 업로드 전 진단까지. Use when a pptx looks different after uploading to Canva, when Canva rejects a pptx that PowerPoint opens fine, or when preparing one deck for both PowerPoint recording and Canva editing. 트리거 — 캔바 업로드, 캔바 글꼴, 굵기가 다르다, 캔바에서 서식 깨짐, 캔바가 파일을 못 읽는다, pptx 캔바 변환, 임베드 글꼴.
---

# pptx-canva-bridge — 같은 pptx 인데 캔바에서 다르게 보일 때

## 결론부터

**한 파일로 파워포인트와 캔바를 동시에 만족시킬 수 없다. 두 벌을 만든다.**

| | 녹화용 원본 | 캔바 업로드용 사본 |
|---|---|---|
| `b="true"` | 그대로 둔다 | 이름에 웨이트가 든 글꼴에서만 뺀다 |
| 임베드 글꼴(`.fntdata`) | 그대로 둔다 | 뺀다 |
| 애니메이션(`p:timing`) | 그대로 둔다 | 뺀다 |

이 파일이 있는 폴더가 `<SKILL>` 이다. 아래 명령의 `<SKILL>` 을 실제 절대경로로 바꿔 쓴다.

```bash
python "<SKILL>/src/check_pptx.py" "교안.pptx"          # 먼저 진단한다
python "<SKILL>/src/for_canva.py"  "교안이_있는_폴더"    # 캔바용 사본을 만든다
```

`for_canva.py` 는 대상 폴더의 `.pptx` 를 읽어 `대상폴더/캔바업로드용/*_캔바용.pptx` 를 만든다.
**원본은 한 바이트도 건드리지 않는다.**

## 굵기가 어긋나는 이유

캔바의 한글 글꼴은 웨이트가 이름 단위로 갈린다. 네모고딕은 가벼운·보통·중간·가장 굵은 넷이다.
pptx 의 run 은 글꼴 이름과 굵기 플래그를 따로 들고 있다.

```xml
<a:rPr lang="ko-KR" sz="3600" b="true">
  <a:latin typeface="네모고딕 Heavy"/>
</a:rPr>
```

이름에 Heavy 가 있는데 `b="true"` 가 겹쳐 있다. **캔바는 이 조합을 받으면 이름의 웨이트를
무시하고 한 단계 아래인 "중간"으로 잡는다.** `b` 를 빼면 이름대로 "가장 굵은"을 쓴다.

파워포인트는 반대로 움직인다. `b` 를 뺀 파일을 파워포인트에서 열면 굵게 보이지 않는다.
그래서 녹화용 원본에서는 `b` 를 절대 빼면 안 된다.

두 동작 모두 2026-09-07 에 실제 파일로 확인했다. 경위는 [docs/실측기록.md](docs/실측기록.md).

### 어떤 글꼴이 대상인가

이름 자체에 웨이트 낱말이 들어 있는 글꼴만 해당한다.

- 대상: `네모고딕 Heavy`, `윤굴림 Bold`, `Glacial Indifference Bold`, `Arimo Bold`, `Pretendard SemiBold`
- 대상 아님: `맑은 고딕`, `Arial`, `나눔고딕` (이름에 웨이트가 없어 b 플래그로만 굵기가 정해진다)

`for_canva.py` 는 이름을 낱말 단위로 훑어 Thin·ExtraLight·Light·Medium·SemiBold·DemiBold·Bold·
ExtraBold·Black·Heavy 와 한글 표기(가는·라이트·미디움·중간·세미볼드·볼드·헤비·굵은)를 찾는다.
`Bookman`, `Blackadder` 처럼 낱말 안에 우연히 겹치는 이름은 걸리지 않는다.
실행하면 어떤 글꼴을 대상으로 잡았는지 목록으로 찍는다. 반드시 눈으로 확인한다.

## 임베드 글꼴

PC 에 설치되지 않은 글꼴이라도 pptx 안에 심어 두면(`ppt/fonts/*.fntdata` +
`ppt/presentation.xml` 의 `<p:embeddedFontLst>`) 파워포인트는 제대로 그리고 편집도 된다.
**설치 안 됐다고 파일이 잘못된 것이 아니다.** 진단 스크립트가 임베드 글꼴 목록을 찍어 준다.

캔바는 임베드 글꼴을 읽지 않는다. 이름으로 자기 라이브러리를 뒤져 다시 맞춘다.
그래서 캔바용 사본에서는 빼는 편이 낫다. 용량도 준다(37장짜리 교안 기준 16.7MB 에서 11.9MB).

**캔바에 없는 글꼴 이름이면 굵기 문제 이전에 글꼴 자체가 바뀐다.** 캔바 쪽 글꼴 이름과
pptx 안의 이름이 같은지부터 확인한다.

## 캔바가 파일을 못 읽을 때 (CRC 손상)

파워포인트는 zip 멤버의 CRC 를 검사하지 않는다. 캔바는 검사하고 거부한다.
그래서 **파워포인트에서 멀쩡히 열리는 파일이 캔바 업로드만 실패**하는 일이 생긴다.

```bash
python "<SKILL>/src/fix_crc.py" "깨진.pptx" "성한.pptx"
```

깨진 멤버만 성한 파일에서 가져와 갈아 끼운다. 나머지 바이트는 그대로 둔다.
슬라이드 수와 발표자 노트가 보존된다. 성한 파일은 자동 저장본이나 백업본이면 된다.
같은 그림이 들어 있기만 하면 된다.

성한 사본이 없으면 손상된 멤버를 확인하고(스크립트가 이름과 크기를 찍는다) 그 그림만
다시 넣은 뒤 저장한다.

## 작업 순서

1. `check_pptx.py` 로 원본을 진단한다. 임베드 글꼴, 겹친 볼드 개수, CRC 상태를 본다.
2. CRC 가 깨졌으면 `fix_crc.py` 로 먼저 고친다. 원본을 고치는 것이다.
3. `for_canva.py` 로 캔바용 사본을 만든다.
4. 사본을 캔바에 올린다. 슬라이드 한 장을 열어 글꼴 패널에서 웨이트 이름을 눈으로 확인한다.
   네모고딕이면 "가장 굵은"이 잡혀 있어야 한다.
5. 녹화는 원본으로 한다.

## 절대 어기면 안 되는 것

- **원본을 덮어쓰지 않는다.** `for_canva.py` 는 항상 새 폴더에 쓴다. 이 동작을 바꾸지 않는다.
- **웨이트가 이름에 없는 글꼴에서 b 를 빼지 않는다.** `맑은 고딕` 같은 글꼴은 b 가 유일한
  굵기 정보다. 빼면 캔바에서도 가늘어진다.
- **캔바에서 편집한 뒤 다시 내려받아 녹화용으로 쓰지 않는다.** 캔바는 내보낼 때 글꼴을
  자기 것으로 다시 굽는다. 굵기가 또 어긋난다.
- **CRC 복구 때 멀쩡한 멤버를 다시 압축하지 않는다.** `fix_crc.py` 는 압축된 바이트를
  그대로 옮긴다. 바뀐 곳이 깨진 멤버 하나뿐이어야 무엇이 달라졌는지 확인할 수 있다.
  복구본과 원본을 멤버 단위로 비교해 차이가 그 하나뿐인지 확인하고 넘긴다.

## 확인 명령

```bash
# 임베드 글꼴이 남아 있나
python -c "import zipfile,sys; z=zipfile.ZipFile(sys.argv[1]); print(sum(1 for n in z.namelist() if n.endswith('.fntdata')))" "파일.pptx"

# 이름에 Heavy 가 있으면서 b 가 겹친 곳 세기
python "<SKILL>/src/check_pptx.py" "파일.pptx"
```
